# Work-specific packages
# Symlink to activate on work machines:
#   ln -s ~/.Brewfile.work ~/.Brewfile.local

brew "openshift-cli"

# Legacy VM tooling for old Vagrant-based workflows
# cask "virtualbox@beta" # Beta version supports Arm processors
# cask "vagrant"

cask "intellij-idea"
cask "cursor"
cask "slack"
